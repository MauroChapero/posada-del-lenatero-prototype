{
  "extends": "stylelint-config-standard",
  "plugins": ["stylelint-config-tailwindcss"],
  "rules": {
    "at-rule-no-unknown": [
      true,
      {
        "ignoreAtRules": [
          "apply",
          "tailwind",
          "layer",
          "config"
        ]
      }
    ]
  }
}
